# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/webeoc_builder"

# Auth (next-auth) — generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"
